project:
  name: sepsis_latent_progression
  seed: 42

data:
  raw_path: data/raw
  processed_path: data/processed

training:
  batch_size: 32
  lr: 0.0001
  epochs: 20

model:
  latent_dim: 64
  lstm_hidden: 128
